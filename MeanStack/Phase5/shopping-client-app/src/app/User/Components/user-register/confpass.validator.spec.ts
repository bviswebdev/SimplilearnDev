import { Confpass.Validator } from './confpass.validator';

describe('Confpass.Validator', () => {
  it('should create an instance', () => {
    expect(new Confpass.Validator()).toBeTruthy();
  });
});
